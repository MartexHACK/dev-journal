# Reviewing a strategy with a second pair of eyes

A second reader catches a different class of error than a test does. Tests check the behaviour you thought to specify; a reviewer questions the specification itself. On research code the highest-value review question is not is this correct but what would this look like if the effect were not real, because that is the failure a passing test cannot surface.
