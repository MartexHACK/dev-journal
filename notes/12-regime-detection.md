# Regimes are labels you impose, not facts you find

Hidden Markov models and clustering will always return regimes because they are asked to. The test is whether the regime label has out-of-sample predictive content for a quantity you care about, evaluated on data after the model was fit. Otherwise it is a description of the past.
